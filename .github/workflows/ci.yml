name: CI

on:
  push:
    branches: [main]
  pull_request:
  # Lets a run be started by hand, for when a trigger is missed.
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          # Read from go.mod so there is no second version to keep in sync.
          go-version-file: go.mod

      # `make` builds the front end in web/ before compiling it in.
      - uses: actions/setup-node@v4
        with:
          node-version-file: web/.nvmrc
          cache: npm
          cache-dependency-path: web/package-lock.json

      - name: gofmt
        run: |
          unformatted=$(gofmt -l .)
          if [ -n "$unformatted" ]; then
            echo "These files need gofmt:"
            echo "$unformatted"
            exit 1
          fi

      - name: vet
        run: go vet ./...

      - name: test
        run: go test ./...

      # The race detector needs cgo, so it cannot run on a stock Windows dev
      # box. This is the only place it reliably executes, which makes it worth
      # having here rather than relying on anyone to run it locally.
      - name: test with the race detector
        run: go test -race ./...

      - name: build
        run: make all
